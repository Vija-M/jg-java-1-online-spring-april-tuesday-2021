package instructor.lesson_3;

class Calculator {

    // sum(x, y) = x + y
    int sum(int x, int y) {
        int r = x + y;
        return r;
    }

    int sqr(int x) {
        return x * x;
    }
}
